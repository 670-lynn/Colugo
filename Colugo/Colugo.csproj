<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <RootNamespace>Colugo</RootNamespace>
    <AssemblyName>Colugo</AssemblyName>
    <Version>0.1.0</Version>
    <Title>Colugo</Title>
    <Description>A Grasshopper plugin for Rhino</Description>
    <GenerateAssemblyInfo>true</GenerateAssemblyInfo>
    <TargetExt>.gha</TargetExt>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Grasshopper" Version="7.13.21348.13001" />
    <PackageReference Include="Microsoft.Office.Interop.Excel" Version="15.0.4795.1001" />
    <PackageReference Include="System.Data.SQLite.Core" Version="1.0.118" />
    <Reference Include="Microsoft.CSharp" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Resources\ReadMe_Icon.png" />
    <EmbeddedResource Include="Resources\Colugo_Logo.png" />
    <EmbeddedResource Include="Resources\GridHarmony_Icon.png" />
  </ItemGroup>

  <!-- Post-build: copy GHA to Grasshopper Libraries folder for debugging -->
  <Target Name="CopyToGrasshopperLibraries" AfterTargets="Build" Condition="'$(Configuration)' == 'Debug'">
    <PropertyGroup>
      <GrasshopperLibDir>$(AppData)\Grasshopper\Libraries\Colugo\</GrasshopperLibDir>
    </PropertyGroup>
    <MakeDir Directories="$(GrasshopperLibDir)" />
    <Copy SourceFiles="$(TargetPath)" DestinationFolder="$(GrasshopperLibDir)" />
    <Copy SourceFiles="$(TargetDir)$(TargetName).pdb" DestinationFolder="$(GrasshopperLibDir)" Condition="Exists('$(TargetDir)$(TargetName).pdb')" />
    <Copy SourceFiles="$(TargetDir)System.Data.SQLite.dll" DestinationFolder="$(GrasshopperLibDir)" Condition="Exists('$(TargetDir)System.Data.SQLite.dll')" />
    <MakeDir Directories="$(GrasshopperLibDir)x64" Condition="Exists('$(TargetDir)x64\SQLite.Interop.dll')" />
    <Copy SourceFiles="$(TargetDir)x64\SQLite.Interop.dll" DestinationFolder="$(GrasshopperLibDir)x64\" Condition="Exists('$(TargetDir)x64\SQLite.Interop.dll')" />
  </Target>

</Project>
